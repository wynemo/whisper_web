name: Deploy to Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  SERVER_HOST: 115.190.2.58
  SERVER_USER: root

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ env.SERVER_HOST }}
          username: ${{ env.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /root/whisper_web
            git pull
            systemctl restart whisper_web
